# MongoDB Web API 配置文件
server:
  host: "0.0.0.0"      # 监听地址
  port: 8080           # 监听端口
  mode: "debug"        # gin 运行模式: debug / release

mongo:
  uri: "mongodb://localhost:27017"   # MongoDB 连接串
  connect_timeout: 10s               # 连接超时
  request_timeout: 30s               # 请求超时